image: alpine/edge
packages:
  - hugo
  - hut
  - just
  - rsvg-convert
oauth: pages.sr.ht/PAGES:RW
environment:
  site: stefan.vanburen.xyz
tasks:
  # Verify the site builds on every branch.
  - build: |
      cd $site
      hugo build --renderToMemory --logLevel warn
  - publish: |
      cd $site
      # Only upload on the main branch.
      if [ "$GIT_REF" != "refs/heads/main" ]
      then
        echo "Not on main branch, not publishing!"
        exit 0
      fi
      just publish
